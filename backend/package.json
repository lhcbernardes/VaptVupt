{
  "name": "vaptvupt-recipe-parser",
  "version": "0.1.0",
  "description": "Backend service that turns blog and Instagram URLs into structured Recipe JSON. Uses yt-dlp + OpenAI Whisper for audio transcription, JSON-LD parsing for structured recipe sites, and Anthropic Claude for final structuring.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@fastify/cors": "^9.0.1",
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "openai": "^4.67.0",
    "redis": "^5.12.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20"
  }
}
