# Required
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# Optional
PORT=3000
LOG_LEVEL=info

# Optional auth (clients send Bearer <APP_API_KEY> in Authorization header).
# Leave blank in dev — required in production to avoid burning API credit.
APP_API_KEY=
